language: generic 
sudo: required
services:
  - docker

before_install:
  - docker build -t sergeishteiner/docker-react -f Dockerfile.dev .


script:
  - docker run -e CI=true sergeishteiner/docker-react npm run test